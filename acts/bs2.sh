# get man bash in file and remove contorl so that we can read
# use cntl v to get the cntl chracter
sed 's/.//' mbash > mtest
$! process number of last background command

# man $@ |col -b > 'bash.1'                                                                                         
